import { CartContent } from '../components/CartContent';

export const CartPage = () => {
  return (
    <main>
      <section className="page">
        <CartContent />
      </section>
    </main>
  );
};
